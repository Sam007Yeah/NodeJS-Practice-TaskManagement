info:
  name: New Task
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4000/createTask
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
