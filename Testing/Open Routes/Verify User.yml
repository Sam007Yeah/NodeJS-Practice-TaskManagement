info:
  name: Verify User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4000/verifyUser
  body:
    type: json
    data: |-
      {
        "id" : "U000",
        "password" : "bob"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
