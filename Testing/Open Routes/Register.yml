info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4000/register
  body:
    type: json
    data: |-
      {
        "username" : "bob",
        "password" : "bob"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
