info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:4000/login
  body:
    type: json
    data: |-
      {
        "id" : "U000",
        "password" : "bob"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
