info:
  name: Health Check
  type: http

http:
  method: GET
  url: http://localhost:4000/health_check
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
